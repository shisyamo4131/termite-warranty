[agents]
enabled = true
max_concurrent_threads_per_session = 4
interrupt_message = true

# Project-owned hybrid configuration. Preserve project-specific permissions and
# Task replacement is user-requested and uses ordinary repository startup.
